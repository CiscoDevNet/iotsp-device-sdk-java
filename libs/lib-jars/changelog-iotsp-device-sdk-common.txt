Feb 27,2018	0.3.1.0	
                Fix set device logic, 
                Add confId to all the ack Message

Mar 05,2018	0.3.2.0	
                  Updating the configuration/command version when an empty device list is sent with the command. 
                  Intializing the device connected state, device reachable state, device admin state to UNKNOWN instead of null.

Mar 06,2018	0.3.3.0	
                  API to remove the supported commands from the GwConfigCache. Once the command is removed, GwConfigCache will throw an
                  an exception when command of that type is used to update cache. Users could add the command back using addSupportedCommandToCache.
